    // MENU SCENE
// -------------------------------------------------------------
// this file controls the menus, like loading and saving games etc.

export class MenuScene extends Phaser.Scene {

    // the unique scene identifier. 
    // individual scene configuration can be passed here as well
    constructor () {
        super('MenuScene');
    }

    create () {
        
    }
}
