// REALTIME INTERFACE
// -------------------------------------------------------------
// Most of the user interface for this game will be realised DOM-based via HTML and CSS, but for the use cases where realtime interface feedback is needed we define all variables in this file

export class RealtimeInterface {

    static colors () {
        let fontColor = '#00ff00';
        return fontColor;
    }

    static fontBig () {
        let fontDefinition = "72px Courier";
        return fontDefinition;
    }
}
